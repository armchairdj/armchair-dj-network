extends /layout/armchair

block content
  article.posts
    - if (headline)
      header
        h1!=headline

    - if (posts.length)
      - each post in posts
        +postPreview(post)
    - else
      p No posts to display.
