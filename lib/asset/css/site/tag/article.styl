article {
  > header {
    marginBottom(40px);
  }

  header,
  section {
    marginBottom(20px);
  }

  header {
    h1,
    h2,
    h3,
    h4,
    h5,
    h6 {
      marginBottom(10px);
      bold();
    }
  }

  section {
    
  }
}