flipBackgroundImage() {
  transform: scaleX(-1);
  filter: FlipH;

  > * {
    transform: scaleX(-1);
    filter: FlipH;
  }
}

backgroundForegroundImage($size) {
  background-size: $size $size;
  min-height: $size * 1.05;
  padding-right: ($size * 1.1);
}
