article.posts {
  > section.post {
    standardRuleLine(#333);

    header.postInfo {
      h2.title {
        font-size: 32px;
      }

      h3.artist {
        font-size: 32px;
      }

      h4.type {
        font-size: 18px;
      }

      h5.year {
        font-size: 22px;
      }
    }
  }
}
