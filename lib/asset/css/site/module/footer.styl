$footerIconSize = 100px;

aside.footer {
  nav {
    background-repeat: no-repeat;
    background-image: url('/image/theme/jet/armchair.jpg');
    background-position: bottom right;
    background-size: $footerIconSize $footerIconSize;
    min-height: $footerIconSize;
    padding-right: ($footerIconSize * 1.2);
  }
}