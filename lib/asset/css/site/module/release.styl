article.release {
  > header {
    h1,
    h2,
    h3,
    h4,
    h5,
    h6 {
      normal();
      breathable();
      font-size: $bodyFontSize;
      margin: 0;

      &.type {
        font-size: 20px;

        &:after {
          content: '\00A0\2192';
        }
      }
      &.artist {
        font-size: 32px;
        color: darken($taglineTextColor, 20);
      }
      &.title {
        font-size: 32px;
        color: $taglineTextColor;
      }
      &.year {
        font-size: 26px;
        color: #999;
      }
    }
  }

  > header,
  > section {
    standardRuleLine(#333);
  }

  &.song {
    > header {
      h1,
      h2,
      h3,
      h4,
      h5,
      h6 {
        &.title cite {
          &:before {
            content: '\201C';
          }
          &:after {
            content: '\201D';
          }
        }
      }
    }
  }
}
